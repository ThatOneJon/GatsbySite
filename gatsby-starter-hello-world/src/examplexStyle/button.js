import styled from "styled-components"

//Styled components can be exported with individual name 
export const ExampleButton = styled.button`
    background: green;
    color: red;
`

// the styles are written in template literals